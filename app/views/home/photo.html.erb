<div class="container">
    <div class="grid">
        <div class="grid-sizer"></div>
      <% @photos.each do |photo| %>
         <div class="grid-item">
            <%= image_tag photo.link, alt: "random" %>
          </div>
      <% end %>
    </div>
</div>
<%= paginate @photos %>
